name: flutter_application_1
description: "BaseApp - Gestión de Clientes"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # HTTP para consumir la API
  http: ^1.2.1
  # Almacenamiento local (para guardar token/sesión)
  shared_preferences: ^2.2.2
  # Iconos extra
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
